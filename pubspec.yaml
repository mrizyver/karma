name: karma

description: A Flutter application to improve karma.

publish_to: "none"

version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #  flutter_svg: ^1.0.3
  provider: ^6.0.2
  http: ^0.13.4
  shared_preferences: ^2.0.15
  google_fonts: ^2.3.2
  Add your other dependencies here

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Add your other dev dependencies here

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
